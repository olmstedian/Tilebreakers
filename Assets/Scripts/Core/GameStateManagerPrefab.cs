using UnityEngine;

/// <summary>
/// Helper script to create a GameStateManager prefab.
/// Attach this script to a GameObject and create a prefab from it.
/// </summary>
[RequireComponent(typeof(GameStateManager))]
public class GameStateManagerPrefab : MonoBehaviour
{
    // No debug messages needed here
}
